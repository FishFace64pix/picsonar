{
  "name": "eventfacematch",
  "version": "1.0.0",
  "description": "AI-Powered Event Photo Delivery System",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "deploy": "cd backend && npm run deploy"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}

